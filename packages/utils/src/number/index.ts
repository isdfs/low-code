/**
 * 限制数字在指定的范围内。
 *
 * @param value - 要限制的数字。
 * @param min - 最小值。
 * @param max - 最大值。
 * @returns 被限制在范围内的数字。
 */
export { clamp } from './clamp';

/**
 * 检查一个值是否为数字。
 *
 * @param value - 要检查的值。
 * @returns 如果是数字则返回 true，否则返回 false。
 */
export { isNumber } from './isNumber';