/**
 * 平滑滚动窗口或元素到目标位置。
 * @param targetPosition - 目标位置（滚动高度）。
 * @param duration - 动画持续时间（毫秒）。
 * @param element - 可选，默认是documentElement，要滚动的元素。
 */
export declare function animateScrollTo(targetPosition: number, duration: number, element?: HTMLElement): void;
